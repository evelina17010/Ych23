<Page x:Class="Ych2.Pages.PartnerHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="История продаж" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#355CBD">
            <StackPanel VerticalAlignment="Center" Margin="20">
                <TextBlock x:Name="txtPartnerName" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="txtPartnerPhone" FontSize="14" Foreground="White"/>
            </StackPanel>
        </Border>
        <ListView x:Name="HistoryLv" Grid.Row="1" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар" Width="300" DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="Кол-во" Width="80" DisplayMemberBinding="{Binding Quantity}"/>
                    <GridViewColumn Header="Сумма" Width="100" DisplayMemberBinding="{Binding Amount}"/>
                    <GridViewColumn Header="Точка" Width="200" DisplayMemberBinding="{Binding Point}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10" VerticalAlignment="Center">
            <TextBlock Text="Всего:" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock x:Name="txtTotal" FontSize="16" FontWeight="Bold" Foreground="#355CBD"/>
        </StackPanel>
        <Button Content="Назад" Name="btnBack" Grid.Row="2" Width="100" Height="35"  Click="btnBack_Click" Background="#355CBD" Foreground="White"  HorizontalAlignment="Left" Margin="10"/>
    </Grid>
</Page>